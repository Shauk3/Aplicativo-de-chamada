import { useState } from "react";

export default function App() {
    return (
        <div className="w-full h-screen bg-black/90 flex items-center justify-center">
            <h1 className="text-3xl font-bold text-white">Vite + React</h1>
        </div>
    );
}